/**
 * The report sections a manager can view side-by-side across the whole
 * team for a single week (the "cross-team single-section view" from the
 * assignment's dashboard bonus requirements).
 */
export enum ReportSection {
  TASKS_COMPLETED = 'TASKS_COMPLETED',
  PLANNED_TASKS = 'PLANNED_TASKS',
  BLOCKERS = 'BLOCKERS',
  ACHIEVEMENTS = 'ACHIEVEMENTS',
  NOTES = 'NOTES',
}
